---
# Tasks for testing role

# SELinux cannot be enabled inside a docker container because it appears
# disabled every time.

- name: Configure sandbox environment
  hosts: localhost
  roles:
    - role: amtega.docker_presets
      docker_presets_images_json_query: >-
        [? starts_with(name, `centos-6`)
        || starts_with(name, `centos-7`)
           || starts_with(name, `fedora-29`)
           || starts_with(name, `fedora-30`) ]
  tags:
    - sandbox

- name: Setup testing sandbox
  hosts: localhost
  roles:
    - role: amtega.docker_sandbox
      docker_sandbox_state: started
      docker_provisioner_container_privileged: true
  tags:
    - sandbox

- name: Test selinux role
  hosts: docker_sandbox_containers
  roles:
    - role: amtega.selinux
      selinux_mode: unchanged
  tags:
    - idempotence

- name: Cleanup testing sandbox
  hosts: localhost
  roles:
    - role: amtega.docker_sandbox
      docker_sandbox_state: absent
  tags:
    - sandbox
